name: Deploy ZemZem to cPanel

on:
  push:
    branches: [main]
    paths-ignore:
      - 'README.md'
  workflow_dispatch:

concurrency:
  group: zemzem-production-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate deployment secrets
        env:
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_PORT: ${{ secrets.FTP_PORT }}
          FTP_SERVER_DIR: ${{ secrets.FTP_SERVER_DIR }}
        run: |
          test -n "$FTP_USERNAME" || (echo "Missing FTP_USERNAME" && exit 1)
          test -n "$FTP_PASSWORD" || (echo "Missing FTP_PASSWORD" && exit 1)
          test -n "$FTP_PORT" || (echo "Missing FTP_PORT" && exit 1)
          test -n "$FTP_SERVER_DIR" || (echo "Missing FTP_SERVER_DIR" && exit 1)

      - name: Deploy files via FTPS control and PASV
        env:
          FTP_SERVER: quebec.us.hostns.io
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_PORT: ${{ secrets.FTP_PORT }}
          FTP_SERVER_DIR: ${{ secrets.FTP_SERVER_DIR }}
        run: |
          upload() {
            local src="$1"
            local dst="$2"
            curl --fail --show-error --silent \
              --connect-timeout 20 --max-time 60 --retry 1 \
              --ftp-ssl-control \
              --disable-epsv \
              --ftp-skip-pasv-ip \
              --ftp-create-dirs \
              --user "$FTP_USERNAME:$FTP_PASSWORD" \
              --upload-file "$src" \
              "ftp://${FTP_SERVER}:${FTP_PORT}${FTP_SERVER_DIR}${dst}"
          }

          upload index.html index.html
          upload shop.html shop.html
          upload checkout.html checkout.html
          upload product.html product.html
          upload admin.html admin.html
          upload assets/css/style.css assets/css/style.css
          upload assets/css/product.css assets/css/product.css
          upload assets/css/admin.css assets/css/admin.css
          upload assets/js/app.js assets/js/app.js
          upload assets/js/app-v2.js assets/js/app-v2.js
          upload assets/js/app-v3.js assets/js/app-v3.js
          upload assets/js/app-v4.js assets/js/app-v4.js
          upload assets/js/admin.js assets/js/admin.js

      - name: Verify live Supabase storefront and admin
        run: |
          sleep 2
          curl -L --fail --silent --show-error --max-time 20 https://www.zemzem.al/ -o /tmp/live.html
          grep -q 'assets/js/app-v4.js' /tmp/live.html

          curl -L --fail --silent --show-error --max-time 20 https://www.zemzem.al/assets/js/app-v4.js -o /tmp/app-v4.js
          grep -q 'fetchCatalog' /tmp/app-v4.js
          grep -q 'storefront_books' /tmp/app-v4.js

          curl -L --fail --silent --show-error --max-time 20 https://www.zemzem.al/shop.html -o /tmp/shop.html
          grep -q 'assets/js/app-v4.js' /tmp/shop.html

          curl -L --fail --silent --show-error --max-time 20 https://www.zemzem.al/checkout.html -o /tmp/checkout.html
          grep -q 'assets/js/app-v4.js' /tmp/checkout.html

          curl -L --fail --silent --show-error --max-time 20 https://www.zemzem.al/product.html -o /tmp/product.html
          grep -q 'assets/js/app-v4.js' /tmp/product.html
          grep -q 'productDetail' /tmp/product.html

          curl -L --fail --silent --show-error --max-time 20 https://www.zemzem.al/admin.html -o /tmp/admin.html
          grep -q 'ZemZem Admin' /tmp/admin.html
          grep -q 'assets/js/admin.js' /tmp/admin.html

          curl -L --fail --silent --show-error --max-time 20 https://www.zemzem.al/assets/js/admin.js -o /tmp/admin.js
          grep -q 'admin_me' /tmp/admin.js
          grep -q 'tamarillot@gmail.com' /tmp/admin.js

          echo 'ZEMZEM_STOREFRONT_AND_ADMIN_OK=yes'
